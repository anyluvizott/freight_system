<!DOCTYPE html>
<html>
  <head>
    <title>Sistema de Frete</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
  </head>
  <body>
    <div id="wrapper">
      <!-- Sidebar -->
      <div id="sidebar-wrapper">
        <ul class="sidebar-nav">
          <li class="sidebar-brand">
            <%= link_to 'Transportadora AML', root_path, :class => "nav-side-transport" %>
          </li>
          <li>
            <%= link_to 'Ordem de Serviço', order_of_services_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <%= link_to 'Modalidades de Transporte', transport_models_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <%= link_to 'Veículos Cadastrados', carriers_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <%= link_to 'Tabela de Preço por Peso', price_by_weights_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <%= link_to 'Tabela de Preço por Distância', price_per_distances_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <%= link_to 'Tabela de Prazo de Entrega', delivery_times_path, :class => "nav-side-transport"  %>
          </li>
          <li>
            <% if user_signed_in? %>
              <%= button_to 'Sair', destroy_user_session_path, method: :delete, :class => "fixed-bottom nav-side-transport login-logout" %>
            <% else %>
              <%= link_to 'Entrar', new_user_session_path, :class => "fixed-bottom nav-side-transport login-logout" %>
            <% end %>
          </li>
        </ul>
      </div>
      <!-- /#sidebar-wrapper -->
      <!-- Page Content -->
      <div id="page-content-wrapper">
        <div class="col-lg-12">
          <main class="my-5">
            <div class="nav-user">
              <% if user_signed_in? %>
                <%= current_user.user %>
              <% end %>
            </div>
            <%= alert %>
            <%= notice %>
            <div class="container">
              <%= yield %>
            </div>
          </main>
        </div>
        <!-- /#page-content-wrapper -->
      </div>
      <!-- /#wrapper -->
    </body>
  </html>
